import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:coocktimer/app/modules/core/core_controller.dart';
import 'package:coocktimer/app/modules/core/core_module.dart';

void main() {
  initModule(CoreModule());
  // CoreController core;
  //
  setUp(() {
    //     core = CoreModule.to.get<CoreController>();
  });

  group('CoreController Test', () {
    //   test("First Test", () {
    //     expect(core, isInstanceOf<CoreController>());
    //   });

    //   test("Set Value", () {
    //     expect(core.value, equals(0));
    //     core.increment();
    //     expect(core.value, equals(1));
    //   });
  });
}
